# SQLite file database (swap to PostgreSQL in production)
DATABASE_URL="file:./dev.db"

# Long random string used to sign login cookies
AUTH_SECRET="replace-with-a-long-random-secret"

# Public site URL (used in emails / Razorpay callbacks later)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Razorpay — paste keys from https://dashboard.razorpay.com/app/keys
NEXT_PUBLIC_RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
RAZORPAY_WEBHOOK_SECRET=""

# Seeded CRM admin (change after first login)
ADMIN_EMAIL="leo.a@example.org"
ADMIN_PASSWORD="Nuvelist@Admin1"
